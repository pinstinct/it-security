# 정보시스템의 범위 및 이해

## Point01. CPU 구조적 이해

### CPU 구조 이해

입력장치로부터 자료를 받아 연산하고 그 결과를 출력장치로 보내는 일련의 과정을 제어 및 조정하는 핵심장치로 사람의 두뇌에 해당

- ALU(연산장치): 산술연산, 논리연산 수행
- Register: 고속 일시 메모리
- Control Unit(제어장치): 명령어 해석
- 내부 CPU 버스: ALU와 레지스터 간의 데이터 이동을 위한 경로

### 버스 시스템(Bus System)

버스는 시스템에 많은 장치를 공유하여 데이터, 주소, 제어 정보를 전달하는 전송 라인이다. 한정된 자원이므로 버스를 획득하기 위한 경합이 많이 발생하는 장치이다.

- 데이터 버스: 시스템 컴포넌트 간 처리 데이터를 전송하기 위한 용도
- 주소 버스: 기억장소의 위치 또는 장치 식별을 지정하기 위한 라인
- 제어 버스: CPU와 기억장치 또는 I/O 장치 사이의 제어 신호를 전송하는 라인

## Point02. 메모리 시스템

### 기억장치 계층구조

⭐️ 기억장치 계층구조

```md
상위 계층
  ↑  속도 빠름
  ↑  비용 높음
  ↑  용량 작음
────────────────────────────
레지스터
캐시
주기억장치
보조기억장치: 실행되기 위해서는 프로그램이나 데이터가 주기억장치에 옮겨져야 한다.
────────────────────────────
  ↓  속도 느림
  ↓  비용 낮음
  ↓  용량 큼
하위 계층
```

메모리 계층구조를 통해 성능 차이를 극복하고, 가격 경쟁력을 확보할 수 있다.


### 캐시 메모리

CPU와 주기억장치의 속도 차이를 극복하기 위해 존재하는 고속의 버퍼 메모리이다.

#### 캐시 메모리 사상(Mapping) 방식

- 직접사상(Direct Mapping): Main Memory를 여러 구역으로 분할해 Cache 슬롯과 매핑
    - 장점: 매핑 절차가 단순하고 신속
    - 단점: 높은 캐시 미스율
- ⭐️ 연관사상(Associate Mapping): Main Memory의 각 블록이 Cache의 어느 슬롯이든 적재 가능
    - 장점: 지역성 높은 접근 시 캐시 적중률 높음
    - 단점: 구현 하드웨어가 복잡하여 구현 비용 상승
- 집합 연관사상(Set Associate Mapping): 직접사상/연관사상 절충 방식으로 캐시와 메모리가 M대 1로 대응
    - 장점: 직접사상과 연관사상의 장점 수용
    - 단점: 구현 비용이 많이 듦

### 캐시 메모리 관리 방식 

CPU는 캐시 메모리에 접근해 연산에 필요한 명령과 데이터를 읽어 들인다. 만약, CPU가 캐시 메모리에 접근할 때 원하는 데이터가 없다면, 캐시 메모리는 주기억장치에 접근하여 데이터를 캐시 메모리에 올려야 한다.

#### 캐시 메모리 인출 방식

- Demand Fetch: 필요 시 캐시 인출
- Pre-Fetch: 예상되는 블록을 미리 패치

#### ⭐️ 캐시 메모리 교체(Replacement) 알고리즘 종류 

종류 | 설명 | 특징
-- | -- | --
Random | 교체될 Page를 임의 선정 | Overhead가 적음 
FIFO(First In First Out) | 캐시 내에 오래 있었던 Page 교체 | 자주 사용되는 Page가 교체될 우려
LFU(Least Frequently Used) | 사용 횟수가 가장 적은 Page 교체 | 최근 적재된 Page가 교체될 우려
LRU(Least Recently Used) | 가장 오랫동안 사용되지 않은 Page 교체 | time stamping에 의한 overhead 존재
Optimal | 향후 가장 참조되지 않을 Page 교체 | 실현 불가능
NUR(Not Used Recently) | 참조 비트와 수정 비트로 미사용 Page 교체 | 최근 사용되지 않은 페이지 교체
SCR(Second Chance Replacement) | 최초 참조 비트 1로 셋, 1인 경우 0으로 셋, 0인 경우 교체 | 기회를 한 번 더 제공

##### 페이지 교체 관리 시 문제점

- Page Fault: 기억장치에 적재되지 않은 Page를 사용하려 할 때 발생
- Demand Paging: 요구될 때에만 Page를 적재하는 방식
- Thrashing: Page 부재가 너무 빈번하게 발생해 CPU 프로세스 수행보다 Page 교체에 더 많은 시간을 소요

### 캐시 메모리 일관성(Cache Coherence)



